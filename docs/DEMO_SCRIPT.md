# Demo Script

Target length: 59.5 seconds for the generated final video.

Fast path: use the generated final video in `docs/demo/aegisops-demo-reel-fixed.mov`, uploaded at https://youtu.be/eAqfwJn9sr8, or re-record it with `docs/VIDEO_SUBMISSION.md`.

## 0:00-0:20

Show the AegisOps dashboard and select `Checkout p95 latency jumped after tax calculator rollout`.

Say: AegisOps is a Qwen-powered production incident autopilot for real business workflows, not a toy chat interface.

## 0:20-0:55

Run the workflow with human approval enabled.

Point out:

- Persistent memory recalls prior rollback lessons.
- Tool layer collects log, metric, change, and policy evidence.
- Qwen diagnosis is generated through the OpenAI-compatible Qwen Cloud path when an API key is present.
- The same tools are exposed through OpenAPI and MCP for custom skill integration.
- The `Judge rubric evidence` panel maps the official 30/30/25/15 criteria to repository proof.

## 0:55-1:35

Show the agent council, evidence, and remediation plan.

Point out:

- Multiple roles disagree and constrain action.
- The plan is reversible and scoped to one cell.
- Verification metrics are defined before execution.

## 1:35-2:05

Disable human approval and run the support PII incident.

Show that the system pauses production mutation and keeps drafts blocked.

## 2:05-2:30

Open `/api/alibaba/proof`, then show `docs/ARCHITECTURE.md` and `reports/eval_report.md`.

Say: The project includes runnable tests, eval and ablation fixtures, a 14-scenario stress benchmark, custom Qwen tool surfaces, deployment proof code, and a public open-source package for judges.

Optional quick evidence shots:

- `agents/aegisops/openapi.yaml`
- `agents/aegisops/cap-manifest.json`
- `reports/ablation_report.md`
